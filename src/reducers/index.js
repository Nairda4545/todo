import { combineReducers } from "redux";

const todoListReducer = (state = {}, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default combineReducers({
    potat: () => {
        return 'potat'
    }
})